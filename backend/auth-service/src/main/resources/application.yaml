spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/authdb
    username: authuser
    password: authpass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate       # ✅ schema from Flyway, not JPA
    properties:
      hibernate:
        format_sql: false
        show_sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: "super-super-long-random-secret-key-change-this-in-prod"
  issuer: "timeeconomy-auth"
  access-token-exp-seconds: 900     # 15 min
  refresh-token-exp-days: 7

userservice:
  base-url: http://localhost:8081

# logging:
#   level:
#     org.springframework.security: DEBUG